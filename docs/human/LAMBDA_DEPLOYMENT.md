# Legacy Backend Deployment

This internal deployment runbook was removed from the public repository during open-source hardening.

If you self-host the legacy analysis backend, keep operational details in a private infrastructure repository instead of this codebase.

Do not commit:

- live Lambda URLs
- IAM usernames, role names, or account identifiers
- service-role credentials
- webhook secrets
- screenshots containing console details
