# AGENTS.md Installation Onboarding Redesign

**Date:** 2026-03-18
**Status:** Draft

## Problem

When users type "install https://github.com/onlycastle/BetterPrompt" in Claude Code, the agent doesn't know to use `/plugin marketplace add`. It falls back to git clone + npm install, which is the contributor workflow, not the end-user installation path.

Additionally, AGENTS.md contains stale Gemini API references (the analysis pipeline now runs entirely through Claude — verified: zero imports of `@google/genai` in source code, only the dependency declaration remains) and duplicated installation content exists across multiple docs.

## Design

### AGENTS.md Rewrite

Replace the current AGENTS.md with a persona-based onboarding flow:

1. **Ask purpose** — Agent asks: "Are you setting this up for yourself as an individual developer, or are you a team manager setting it up for your team?"
2. **Install per persona** — Two self-contained paths:
   - **Individual Developer:** Plugin install only (2 commands, no server/keys/cloning)
   - **Team Manager:** Plugin install + clone repo + start dashboard server
3. **Soft onboarding** — Each path ends with a "What to do next" sentence:
   - Individual: "Run `/bp-analyze` to start your first analysis"
   - Team Manager: "Visit `/dashboard/enterprise/setup` to create your org and invite members"

### Content Removed from AGENTS.md

| Removed | Reason |
|---------|--------|
| `GOOGLE_GEMINI_API_KEY` references | Gemini SDK not imported anywhere; analysis runs through Claude  |
| Environment variables tables | All variables are optional; unnecessary for installation |
| Supported Session Sources table | Removed from installation context — Cursor has no plugin system. Note: the plugin still scans Cursor session logs if present; this is a runtime feature, not an installation concern. |
| Enterprise setup walkthrough | Collapsed to "visit the web wizard" — the UI handles it |
| Common Issues table | Removed with env vars; no Gemini-related issues remain |

### Deduplication Across Docs

| File | Change |
|------|--------|
| CLAUDE.md "Installation" section | Replace inline commands with pointer to AGENTS.md |
| CLAUDE.md "Language Policy" | Update "Gemini, Anthropic, etc." to just "Anthropic, etc." |
| README.md | Keep as-is (human-facing, different audience) |
| docs/human/PLUGIN.md | Keep as-is (human dev docs) |
| Frontend components | Out of scope (in-app UI) |

### Gemini Cleanup (Bundled)

| File | Change |
|------|--------|
| `package.json` | Remove `@google/genai` dependency (verified: zero source imports) |
| `next.config.ts` | Remove `@google/genai` from `serverExternalPackages` |

Note: `package-lock.json` will be regenerated by `npm install` after the dependency removal. Stale Gemini references in source code (~40 occurrences across 12 model files, including an `EvidenceVerifierConfig` with `gemini-3-flash-preview` default in `evidence-verification-data.ts`) are excluded from this scope — recommend a follow-up cleanup task with full inventory.

### Final AGENTS.md Content

```markdown
# AGENTS.md

> Machine-readable setup and usage guide for AI coding agents.

## Installation

When a user asks to install BetterPrompt, first ask:

> "Are you setting this up for yourself as an individual developer, or are you a team manager setting it up for your team?"

Then follow the matching path below.

### Individual Developer

Install the Claude Code plugin. No server, no API keys, no cloning needed.

```
/plugin marketplace add onlycastle/BetterPrompt
/plugin install betterprompt@betterprompt
```

**What to do next:** Run `/bp-analyze` in any Claude Code session. The plugin queues analysis automatically when enough new sessions accumulate. Reports are served at `http://localhost:3456`.

### Team Manager

Team managers are also individual developers. Start with the plugin, then add the shared dashboard.

**1. Install the plugin**

```
/plugin marketplace add onlycastle/BetterPrompt
/plugin install betterprompt@betterprompt
```

**2. Start the dashboard server**

```bash
git clone https://github.com/onlycastle/BetterPrompt.git
cd BetterPrompt
npm install
npm run dev
```

Wait for `Ready on http://localhost:3000`.

**What to do next:** Visit `/dashboard/enterprise/setup` to create your organization and invite team members. Share the server URL with your team — each member needs to configure `BETTERPROMPT_SERVER_URL` in their plugin settings and use `sync_to_team` to upload their analysis results.

## Verification

In Claude Code, run `/help` and confirm BetterPrompt appears as an installed plugin with skills (e.g., `analyze-sessions`) and MCP tools (e.g., `scan_sessions`, `save_stage_output`).
```

## Scope

### In Scope

- Rewrite AGENTS.md with persona-based onboarding flow
- Update CLAUDE.md installation section to point to AGENTS.md
- Update CLAUDE.md Language Policy to remove Gemini reference
- Remove `@google/genai` from package.json and next.config.ts
- Run `npm install` to regenerate package-lock.json

### Out of Scope

- README.md changes (human-facing, independent)
- docs/human/ changes (human dev docs, independent)
- Frontend component changes (in-app UI copy)
- Stale Gemini comments in source code (cosmetic — recommend follow-up task)

## Acceptance Criteria

- `npm run typecheck` passes
- `npm run build` passes (confirms no runtime dependency on `@google/genai`)
- `npm test` passes (no test depends on removed `@google/genai` dependency)
- `grep -r "gemini\|GEMINI\|Gemini" AGENTS.md` returns zero results
- CLAUDE.md Installation section points to AGENTS.md, does not duplicate the full flow
- CLAUDE.md Language Policy no longer references Gemini
